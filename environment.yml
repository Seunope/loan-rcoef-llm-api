name: rcoef-api
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - fastapi
  - uvicorn
  - scikit-learn=1.2.2
  - numpy
  - pandas
  - openai
  - joblib
  - pydantic
  - python-dotenv
  - pytest
  - requests
  - gunicorn  # Optional, for production deployments
  - pip
  - pip:
    - modal